import { ImageChangePipe } from './image-change.pipe';

describe('ImageChangePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageChangePipe();
    expect(pipe).toBeTruthy();
  });
});
